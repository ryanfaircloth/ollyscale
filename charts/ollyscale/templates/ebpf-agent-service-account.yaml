{{- if .Values.ebpfAgent.enabled -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "tinyolly.fullname" . }}-ebpf-agent
  namespace: {{ .Values.ebpfAgent.namespace }}
  labels:
    {{- include "tinyolly.labels" . | nindent 4 }}
    app.kubernetes.io/component: ebpf-agent
{{- end }}
