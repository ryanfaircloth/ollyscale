# Minimal test values for chart-testing
# This installs a single-instance PostgreSQL cluster suitable for CI testing

namespace: ollyscale-postgres-test

numberOfInstances: 1

volume:
  size: 1Gi
  storageClass: ''

resources:
  requests:
    cpu: 50m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 512Mi
