{{- if .Values.agentCollector.enabled -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: agent
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "ollyscale-otel.labels" . | nindent 4 }}
    app.kubernetes.io/component: otel-agent
{{- end }}
