{{- if .Values.ebpfAgent.enabled -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "ollyscale-otel.fullname" . }}-ebpf-agent
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "ollyscale-otel.labels" . | nindent 4 }}
    app.kubernetes.io/component: ebpf-agent
{{- end }}
